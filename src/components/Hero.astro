<section class="hero">
  <div class="hero-container">
    <div class="hero-image">
      <img src="/img/profile.jpg" alt="Profile photo" />
    </div>
    <div class="hero-content">
      <p class="hero-role">IT Project Manager</p>
      <h1 class="hero-name">Todsaporn Wattanaiabpan</h1>
      <p class="hero-bio">
        A results-driven and versatile professional, I possess the unique
        'three-in-one' expertise sought in modern IT leadership: Project
        Manager, Software Developer, and Business Analyst. With a proven
        background in code development, I offer a deep technical understanding
        that bridges the gap between client needs and technical execution.
        Experienced in end-to-end project development—from initial business
        concept and requirements analysis through to strategic development,
        final delivery, and successful adoption. I am consistently recognized
        for my ability to coordinate cross-functional teams, thrive in
        deadline-driven environments, and steer complex projects to timely
        completion, always within scope and budget.
      </p>
      <div class="hero-buttons">
        <a
          href="/TODSAPORN-WATTANAIABPAN_1.0.0.pdf"
          download
          class="btn btn-primary">Download CV</a
        >
        <a href="mailto:wat.todsaporn@gmail.com" class="btn btn-secondary"
          >Contact</a
        >
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 4rem 2rem;
    background-color: var(--bg-color);
    min-height: 45vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-container {
    max-width: 1200px;
    width: 100%;
    display: flex;
    align-items: center;
    gap: 4rem;
    flex-wrap: wrap;
  }

  .hero-image {
    flex-shrink: 0;
  }

  .hero-image img {
    width: 300px;
    height: 300px;
    border-radius: 50%;
    object-fit: cover;
    border: 4px solid var(--bg-color);
    box-shadow: 0 2px 8px var(--shadow-color);
    filter: brightness(1.1);
  }

  .hero-content {
    flex: 1;
    min-width: 300px;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .hero-role {
    font-size: 1rem;
    color: var(--text-tertiary);
    font-weight: 300;
    margin: 0;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .hero-name {
    font-size: 3rem;
    font-weight: 700;
    color: var(--text-color);
    margin: 0;
    line-height: 1.2;
  }

  .hero-bio {
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
    max-width: 600px;
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
    margin-top: 0.5rem;
    flex-wrap: wrap;
  }

  .btn {
    padding: 0.75rem 2rem;
    font-size: 0.875rem;
    font-weight: 500;
    border: 2px solid;
    background-color: var(--bg-color);
    cursor: pointer;
    transition: all 0.2s ease;
    font-family: 'SukhumvitSet', sans-serif;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .btn-primary {
    color: var(--color-primary);
    border-color: var(--color-primary);
  }

  .btn-primary:hover {
    background-color: var(--color-primary);
    color: var(--bg-color);
  }

  .btn-secondary {
    color: var(--text-tertiary);
    border-color: var(--text-tertiary);
  }

  .btn-secondary:hover {
    background-color: var(--text-tertiary);
    color: var(--bg-color);
  }

  @media (max-width: 768px) {
    .hero {
      padding: 2rem 1rem;
    }

    .hero-container {
      flex-direction: column;
      gap: 2rem;
      text-align: center;
    }

    .hero-image img {
      width: 250px;
      height: 250px;
    }

    .hero-name {
      font-size: 2rem;
    }

    .hero-content {
      align-items: center;
    }

    .hero-bio {
      text-align: left;
    }

    .hero-buttons {
      justify-content: center;
    }
  }
</style>
